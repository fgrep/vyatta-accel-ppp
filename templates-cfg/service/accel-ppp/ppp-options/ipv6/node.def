type: txt
help: Specifies IPv6 (IPCP) negotioation algorithm.

syntax:expression: $VAR(@) in "deny", "allow", "prefer", "require"

val_help: deny ; don't negotiate IPv6
val_help: allow ; negotiate IPv6 only if client requests
val_help: prefer ; ask client for IPv6 negotiation, don't fail if he rejects
val_help: require ; require IPv6 negotiation
